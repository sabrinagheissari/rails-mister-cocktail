<%= simple_form_for [@cocktail, @dose] do |f| %>
  <%= f.input :description %>
  <%= f.association :ingredient, label: "Choose an ingredient" %>
  <%= f.submit "add to #{@cocktail.name}", class: "new_dose_button" %>
<% end %>
